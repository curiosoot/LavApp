@model IEnumerable<Proyecto_LavApp.Models.marca_vehiculos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Crear")
</p>
<table class="table table-bordered table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.txt_desc_marca)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sn_activo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.txt_desc_marca)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sn_activo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.id_marca_vehiculos }) |
            @Html.ActionLink("Detalles", "Detalle", new { id=item.id_marca_vehiculos }) |
            @Html.ActionLink("Eliminar", "Eliminar", new { id=item.id_marca_vehiculos })
        </td>
    </tr>
}

</table>
